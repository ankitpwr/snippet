version: "3.8"

services:
  backend:
    build: ./server
    container_name: snippetbin_backend
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env

  frontend:
    build: ./client
    container_name: snippetbin_frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
